package com.plantarsas.gestiondocumental.usuarios.entity;

/**
 * Estado de la cuenta de un usuario: activo (puede iniciar sesión),
 * inactivo o bloqueado (no puede).
 */
public enum EstadoUsuario {
    ACTIVO,
    INACTIVO,
    BLOQUEADO
}
